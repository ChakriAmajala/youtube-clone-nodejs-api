pipeline {
    agent any

    environment {
        DOCKERHUB_USER = 'chakriamajaladocker'
        IMAGE_NAME = 'youtube-node-api'
        IMAGE_TAG = 'latest'
        HOST_PORT = "8001"         // Port accessible externally
        APP_PORT = "4000"          // Node.js app port inside container
    }

    stages {

        stage('Checkout') {
            steps {
                git branch: 'master', url: 'https://github.com/ChakriAmajala/youtube-clone-nodejs-api.git'
            }
        }

        stage('Install Dependencies') {
            steps {
                sh '''
                    echo "Installing Node.js dependencies..."
                    npm install
                '''
            }
        }

        stage('Run Tests (optional)') {
            steps {
                sh '''
                    echo "Running tests..."
                    npm test || echo "No test script defined"
                '''
            }
        }

        stage('Build Docker Image') {
            steps {
                sh "docker build -t ${DOCKERHUB_USER}/${IMAGE_NAME}:${IMAGE_TAG} ."
            }
        }

        stage('Login to DockerHub') {
            steps {
                withCredentials([usernamePassword(credentialsId: 'dockerhub-cred', usernameVariable: 'USER', passwordVariable: 'PASS')]) {
                    sh "echo \$PASS | docker login -u \$USER --password-stdin"
                }
            }
        }

        stage('Push Docker Image') {
            steps {
                sh "docker push ${DOCKERHUB_USER}/${IMAGE_NAME}:${IMAGE_TAG}"
            }
        }

stage('Deploy Container') {
    steps {
        withCredentials([string(credentialsId: 'mongo-uri', variable: 'MONGO_URI')]) {
            sh """
                docker rm -f ${IMAGE_NAME} || true
                docker run -d --name ${IMAGE_NAME} \
                -p ${HOST_PORT}:${PORT} \
                -e PORT=${PORT} \
                -e MONGO_URI=${MONGO_URI} \
                ${DOCKERHUB_USER}/${IMAGE_NAME}:${IMAGE_TAG}
            """
        }
    }
}
    } // end of stages

    post {
        always {
            echo "Pipeline finished and updated! Check http://<YOUR_SERVER_IP>:${HOST_PORT}"
        }
    }
}
